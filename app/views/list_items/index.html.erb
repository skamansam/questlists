<h1>Listing list_items</h1>

<table>
  <tr>
    <th>List</th>
    <th>Title</th>
    <th>Description</th>
    <th>Location</th>
    <th>Template</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @list_items.each do |list_item| %>
  <tr>
    <td><%= list_item.list %></td>
    <td><%= list_item.title %></td>
    <td><%= list_item.description %></td>
    <td><%= list_item.location %></td>
    <td><%= list_item.template %></td>
    <td><%= link_to 'Show', list_item %></td>
    <td><%= link_to 'Edit', edit_list_item_path(list_item) %></td>
    <td><%= link_to 'Destroy', list_item, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New List item', new_list_item_path %>
