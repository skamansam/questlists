= simple_form_for(@list) do |f|
  - if @list.errors.any?
    #error_explanation
      %h2 #{pluralize(@list.errors.count, "error")} prohibited this list from being saved:
      %ul
        - @list.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.input :name
  .field
    = f.association :template
  .field
    = f.input :info
  .field
    = f.association :parent 
  -#.field
    = f.association :external_links
  .actions
    = f.submit
