<%= form_for(@tag_icon) do |f| %>
  <% if @tag_icon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_icon.errors.count, "error") %> prohibited this tag_icon from being saved:</h2>

      <ul>
      <% @tag_icon.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag %><br />
    <%= f.text_field :tag %>
  </div>
  <div class="field">
    <%= f.label :icon %><br />
    <%= f.text_field :icon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
