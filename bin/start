mkdir -p "log"
echo "starting firebase server on port 5000..."
firebase-server -v -p 5000 -r database.rules.json -f test_db.json &> log/firebase-server.log &
echo "Done."
echo "Serving files via polymer serve..."
polymer serve
